import React from 'react'
import T from 'prop-types'
// If you create styled components, put them in the <%= props.style %>.js file
// import { StyledComp } from "./<%= props.style %>"

/**
 * <%= props.name %>
 *
 * @class
 */
export class <%= props.name %> extends React.Component {
  static propTypes = {
  }

  static defaultProps = {
  }

  state = {}

  render() {
    return(
      <div>
        <%= props.name %>
      </div>
    )
  }
}
export default <%= props.name %>
