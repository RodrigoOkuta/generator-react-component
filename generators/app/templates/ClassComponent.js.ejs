import React, { Component, Fragment } from 'react'

// see https://reactjs.org/docs/typechecking-with-proptypes.html :
import { array, bool, func, number, string, objecxt, symbol, node, element, elementType, instanceOf, ondOf, oneOfType, arrayOf, objectOf, shape, exact, any } from 'prop-types'

// import styled from "styled-components"
// import { StyledComp } from "./<%= props.subdir ? "style.js" :  props.style %>"

/**
 * <%= props.name %>
 *
 * @class
 */
export class <%= props.name %> extends Component {
  static propTypes = {}

  state = {}

  render() {
    return(
      <Fragment>
        <%= props.name %>
      </Fragment>
    )
  }
}
export default <%= props.name %>
