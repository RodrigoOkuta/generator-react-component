import React from 'react'
import { shallow } from 'enzyme'
import <%= name %> from './<%= name %>'

describe("<%= name %>", () => {
  it("renders the <%= name %> component", () => {
    const actual = shallow(<<%= name %>/>)
    expect(actual).toMatchSnapshot()
  })
})
