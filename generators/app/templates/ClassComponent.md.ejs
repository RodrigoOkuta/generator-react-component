## <%= props.name %>

This will be displayed in the top of the story, providing documentation for your component.

Even better, if you install jsdoc and jsdoc-to-markdown:

    npm install -g jsdoc jsdoc-to-markdown

*And* you write jsdoc-style documentation in your component, you can extract it and replace
this file with the output:

    jsdoc2md <%= props.subdir ? "index.js" : props.name + ".js" %> > <%= props.subdir ? "README.md" : props.name + "md" %>
