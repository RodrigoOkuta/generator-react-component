import React from 'react'
import PropTypes from 'prop-types'

export class <%= name %> extends React.Component {
  constructor(props) {
    super(props)
  }

  render() {
    return(
      <div>

      </div>
    )
  }
}

<%= name %>.propTypes = {}
<%= name %>.defaultProps = {}

export default <%= name %>
