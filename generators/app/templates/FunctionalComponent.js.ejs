import React, { useState, useEffect, useContext, useReducer, useCallback, useMemo, useRef, useLayoutEffect } from 'react'
// see https://reactjs.org/docs/typechecking-with-proptypes.html :
import { array, bool, func, number, string, objecxt, symbol, node, element, elementType, instanceOf, ondOf, oneOfType, arrayOf, objectOf, shape, exact, any } from 'prop-types'
// If you create styled components, put them in the <%= props.style %>.js file
// import { StyledComp } from "./<%= props.style %>"

/**
 * <%= props.name %>
 *
 * @function
 */
export const <%= props.name %> = props => {
  return (
    <>
      Your <%= props.name %> content goes here.
    </>
  )
}

<%= props.name %>.propTypes = {};
<%= props.name %>.defaultProps = {};
export default <%= props.name %>
